@page "/thank-you"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]
@inject NavigationManager NavigationManager

<PageTitle>Thank you - Cheese Trap</PageTitle>

<div class="card bg-light text-center">
    <div class="card-body">
        <div class="container-fluid">
            <div class="row justify-content-end">
                <div class="col">
                    <h3>Thank you for shopping in the Cheese Trap!</h3>
                    <p>Your order has been successfully placed.</p>
                    <img src="/img/happy_cheese.png" alt="Happy cheese" class="img-thumbnail thankyou-img mb-2">
                    <div>
                        <button @onclick="GoToStore" class="btn btn-outline-primary">Return to the Cheese Trap</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private void GoToStore()
    {
        NavigationManager.NavigateTo("/");
    }
}